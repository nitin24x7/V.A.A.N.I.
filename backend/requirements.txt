# VAANI Backend Dependencies
# Python >= 3.10 required (tested on Python 3.12)

# Web & API Framework
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
websockets>=12.0
pydantic>=2.6.0

# Scientific & Signal Processing
numpy>=1.26.0
scipy>=1.12.0

# Deep Learning (CPU-optimized build)
# To install CPU-only PyTorch (recommended for fast, lightweight install):
# pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
torch>=2.2.0
torchaudio>=2.2.0

# Speech-to-Text (ASR)
faster-whisper>=1.0.0

